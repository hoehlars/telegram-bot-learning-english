package commands.conditionals;

import bot.MessengerSingleton;

public class ConditionalStartState implements ConditionalState {
    @Override
    public void onEntry() {

    }

    @Override
    public void doAction() {
        /*messengerSingleton.sendMessageToUser("Welcome to the conditionals game!");
        messengerSingleton.sendMessageToUser("I'll give you a sentence with two words missing. Give me the first missing word first and after that" +
                "the second one! Have fun!");
        messengerSingleton.sendMessageToUser("If you want to skip a word type next (only possible after three tries!)");
        messengerSingleton.sendMessageToUser("Here is your first exercise: ");
        context.setState(new ConditionalAskState());
        context.getState().onEntry();*/
    }
}
